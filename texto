/* Contador carrito */
const contador = document.getElementById("contadorCarrito");

function actualizarContador() {
  const productos = JSON.parse(localStorage.getItem("productos")) || [];
  contador.textContent = productos.length;
}

actualizarContador();

/* Botones */
document.addEventListener("DOMContentLoaded", () => {

  const botones = document.querySelectorAll(".btn-comprar");

  botones.forEach(boton => {
    boton.addEventListener("click", () => {
      const id = boton.dataset.id;

      agregarAlCarrito(id);

      console.log("Click en producto:", id);
    });
  });

});

 /**cuenta carrtio  */
const cuentaCarritoElement = document.getElementById("contadorCarrito");
function actualizarNumeroCarrito(){
    const memoria=JSON.parse (localStorage.getItem("productos"))|| [];
    const cuenta = memoria.reduce ((acum, current)=> acum+current.cantidad,0);
    if (cuentaCarritoElement) {
        cuentaCarritoElement.innerText = cuenta

}


    <div>    
                <button class="restar">-</button>
                <span class="cantidad">${taza.cantidad || 1}</span>
                <button class="sumar">+</button>
            </div>

            <article class="contenedorflearticle">
                <h3 class="Index-h3productos">TAZA INFO 5</h3>
                <p class="productoparrafo">$5000</p>
                <p class="productoparrafo">0</p>
                <button class="buttonEliminar" id="1"> Eliminar</button>
                <p class="productoparrafo" >-----------------</p>
            </article>